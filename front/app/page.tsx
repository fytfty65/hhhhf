'use client';

import ContextualLobby from './ContextualLobby';

// Next.js route entrypoint. Product orchestration lives in its own domain
// module so auth, room and planning concerns can be split independently.
export default ContextualLobby;
